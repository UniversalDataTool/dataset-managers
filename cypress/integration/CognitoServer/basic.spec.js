const CognitoDatasetManager = require("../../../dist/CognitoDatasetManager.js")

describe("basic test", () => {
  it("Should do something", () => {
    cy.wrap(null).then(async () => {
      expect(true).to.equal(true)
    })
  })
})
